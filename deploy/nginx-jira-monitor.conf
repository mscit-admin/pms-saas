# مراقب جيرا — Nginx reverse proxy
# انسخه إلى /etc/nginx/sites-available/jira-monitor ثم فعّله بـ symlink.
# يمرّر الطلبات إلى خادم Next.js على المنفذ 4445.

server {
    listen 80;
    server_name your-domain.com;   # غيّره إلى نطاقك أو عنوان الخادم

    location / {
        proxy_pass http://127.0.0.1:4445;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
